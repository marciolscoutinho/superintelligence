class FeedbackSystem:
    def apply_feedback(self, agent_id, action):
        print(f"Feedback applied to agent {agent_id} for action: {action}")

    def adjust_agent_parameters(self, agent_id):
        print(f"Adjusting parameters for agent {agent_id} due to low performance.")
